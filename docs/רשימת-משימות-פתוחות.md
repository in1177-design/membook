# רשימת משימות פתוחות — לתעדוף משותף

מסמך זה ממזג שני מקורות:
1. סריקה יסודית של הקוד (`app/`, `lib/`, `prisma/`) ושל `docs/` שבוצעה ב-10.8.2026 (תוכננה בזמנו
   כתוכנית בשם `sprightly-wishing-allen.md`, אך מעולם לא נשמרה כקובץ — עד עכשיו).
2. פריט נוסף שעלה בשיחה ב-11.8.2026 (כפתור עריכה/שמירה נפרד לכל כרטיס בדף ההגדרות).

**סטטוס עודכן ונבדק ישירות מול הקוד ב-11.8.2026** — ראו סימון ✅/❌ ליד כל פריט. העבודה שבוצעה
בשיחה של ה-11.8 (שלב 5, שלב 4 תמונה, טבלת מוזמנים, כרטיסי הגדרות) הייתה כולה UI/UX בצד ה-guest
wizard ודף האדמין ולא נגעה באף אחד מהפריטים למטה — כולם עדיין פתוחים חוץ אם צוין אחרת.

אין בקובץ הזה שינוי קוד — זו רשימה בלבד, בלי סדר עדיפות מטעמי, מאורגנת לפי תחום כדי לתעדף ביחד.

## א. באגים ותיקונים קטנים

1. ❌ **`AnswerQuestionStep.tsx` חסר `dir=`** — בניגוד ל-`BlessingStep.tsx` (שכן קובע `dir` לפי
   שפה), טקסט תשובות ברוסית/אנגלית עלול להיות מיושר לא נכון (RTL/LTR). **נבדק ישירות בקוד
   (11.8.2026, שורה 30 של הקובץ) — עדיין חסר.**
2. ❌ **אין דרך לאפס "מגיע/ה לאירוע" בחזרה ל"לא נקבע"** — `AttendingCell` ב-`InviteesTable.tsx`
   מציע רק כן/לא/אולי, בלי אפשרות ניקוי.
3. ❌ **ייצוא ZIP שביר** (`app/api/admin/projects/[id]/zip/route.ts`) — הבאת כל תמונה מ-Cloudinary
   בלי `try/catch` (נכס אחד שנכשל/נמחק מפיל את כל הבקשה ב-500), וכל קבצי התמונה נטענים לזיכרון בבת
   אחת (סיכון timeout/memory בפרויקטים גדולים).
4. ❌ **תמונות יתומות ב-Cloudinary** — מחיקת פרויקט/מוזמן (`deleteProject`, `deleteInvitee` ב-
   `lib/actions.ts`) מוחקת רק מה-DB, לא קוראת ל-Cloudinary `destroy` (לעומת מחיקת תמונה בודדת שכן
   עושה זאת) — אחסון מצטבר לאורך זמן.

## ב. אבטחה ואמינות

5. ❌ **התחברות אדמין** — השוואת סיסמה לא timing-safe (`===` רגיל ב-`lib/auth.ts`), ואין הגבלת
   קצב או נעילה אחרי ניסיונות כושלים.
6. ❌ **אין ולידציה על גודל/סוג קובץ בהעלאת תמונה** — לא בצד לקוח, לא בחתימת Cloudinary
   (`allowed_formats`/`max_file_size` חסרים בכל נתיבי החתימה). ה-PRD המקורי דרש דחיה ברורה מעל
   15MB — לא ממומש בפועל, רק טקסט המלצה לא-אכיף.
7. ❌ **אין rate limiting על נקודת ההגשה הציבורית** (`/api/submissions`) — מתוכנן במפורש במסמך
   הארכיטקטורה, לא מומש.

## ג. תכונות שהתחילו אך לא הושלמו

8. ❌ **מצב "כולם עונים על כל השאלות" (ALL) לא נתמך בכלי בניית הטופס** — האשף האמיתי למוזמן תומך
   בו במלואו, אבל `BuildPage.tsx` בנוי רק על מבנה ה-PICK_ONE. עדיין מנוטרל מבחירה ב-
   `QuestionModeSelect.tsx` (`disabled`, "בקרוב — כלי הבנייה עדיין לא תומך במצב הזה") — **נבדק
   ישירות בקוד, ללא שינוי מ-10.8**.
9. ❌ **אין שליחת הודעות אמיתית** — יש רק "יצירת טקסט + העתקה ללוח" (WhatsApp/מייל). **עודכן
   ב-11.8**: נוסף כפתור העתקת קישור אישי ליד כפתור הוואטסאפ (נוחות עתקה), אבל עדיין אין שליחה
   אוטומטית ואין אפילו קישורי `wa.me`/`mailto:` — הכל ידני לגמרי, כפי שהיה.
10. ❌ **אין תזכורות אוטומטיות** למוזמנים שלא פתחו/לא הגישו — "נשלח" בטבלה הוא רק תיוג ידני של
    האדמין, לא שולח בפועל.

## ד. הסכימה כבר תומכת, המימוש לא הושלם

11. ❌ **תמיכה בכמה תמונות למוזמן** — מודל ה-DB בנוי כ-one-to-many (מוכן להרחבה), אבל
    `app/api/submissions/route.ts` עדיין עושה `mediaAsset.deleteMany` לפני `create` בכל הגשה
    (שורות 128-129) — בפועל תמונה אחת בלבד, ללא שינוי מ-10.8. **שימו לב**: `תיעוד-מצב-נוכחי.md`
    טוען בטעות שזה כבר נתמך — כדאי לתקן את המסמך.
12. ❌ **מודל Organization (ריבוי לקוחות/B2B) קיים בסכימה אך לא בשימוש בכלל** — נבדק: אין אף שאילתה
    בקוד האפליקציה מחוץ ל-`schema.prisma` עצמו.

## ה. עתידי — לא התחיל (מתועד במסמכים כ"בהמשך")

13. ❌ הפקת אלבום מודפס בפועל מהחומרים שנאספו — קיים רק לוח סידור גרירה (`AlbumDesignBoard.tsx`),
    אין export/הדפסה/PDF בפועל.
14. ❌ עוזר כתיבה מבוסס AI (בדיקת איות/ניסוח, אופציונלי למוזמן) — לא קיים בכלל.
15. ❌ לוקליזציה מלאה של ממשק (לא רק תוכן) — מבנה הצ'רום קבוע בעברית/RTL תמיד.
16. ❌ שיוך אוטומטי לתבנית עיצוב (auto template assignment) — לא קיים.
17. ❌ בדיקת איכות תמונה מבוססת AI — לא קיים.
18. ❌ אפליקציית מובייל שצורכת את ה-API — לא קיים.

## ו. תשתית

19. ❌ **אין שום בדיקות אוטומטיות בפרויקט** — לא Jest/Vitest/Playwright, אין `test script` ב-
    package.json, אין קובצי טסט בכלל.
20. ❌ **אין ניטור/התראות על מכסת אחסון Cloudinary** — מחמיר את סעיף 4 (תמונות יתומות) לאורך זמן
    בלי שום נראות.

## ז. נוסף מהשיחה ב-11.8.2026

21. ⏸️ **כפתור "עריכה"/"שמירה" נפרד לכל כרטיס** בדף הגדרות הפרויקט (במקום כפתור אחד גלובלי לכל
    הטופס) — **נדחה ביודעין**, לא "פתוח" במובן הרגיל. תועד גם ב-`מפת-דרכים.md` (Later). הכרטיסים
    כרגע חלק מ-`<form>` אחד ופעולת שרת אחת (`updateProjectDetails`) שקוראת את כל השדות ומוחקת
    שאלות שלא נשלחו מחדש; פיצול אמיתי ובטוח דורש 2-3 פעולות שרת צרות (כמו התבנית הקיימת של
    `updateProjectQuestions`) ולא רק שינוי ב-UI. שווה לבצע רק אם בפועל חוזרים ועורכים כרטיס בודד
    לעיתים קרובות ומוצאים את זה מסורבל.
22. ❌ **הוספת עמודת "פתיחה ראשונה" ו"פתיחה אחרונה" (שעה+תאריך) לטבלת המוזמנים** — הסכימה כבר
    תומכת: `InviteLink.firstViewedAt` / `lastViewedAt` (`prisma/schema.prisma` שורות 235-236) כבר
    נכתבות בכל צפייה אמיתית (`app/i/[token]/page.tsx`), אך `InviteesTable.tsx` לא מציג אותן כעמודה
    — רק נעשה בהן שימוש עקיף לקביעת סטטוס "נצפה". דורש רק שינוי UI: שליפה כבר קיימת
    (`invitee.inviteLink` נטען), רק להוסיף עמודה שמציגה את התאריך/שעה בפורמט קריא.

## מקרא

- ✅ בוצע ואומת בקוד
- ❌ עדיין פתוח (אומת ישירות מול הקוד ב-11.8.2026)
- ⏸️ נדחה ביודעין (החלטה מודעת, לא שכחה)
